import {PartTextFilterPipePipe} from './part-text-filter.pipe';

describe('PartTextFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PartTextFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
