import {PartRequirementFilterPipe} from './part-requirement-filter.pipe';

describe('PartRequirementFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PartRequirementFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
